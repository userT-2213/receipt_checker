# レシートチェック＆支出管理アプリ

PBL（課題解決型学習）演習として開発した、レシート画像解析機能付きのクロスプラットフォーム（Web/モバイル対応）支出管理アプリケーションです。
手動での支出登録に加え、ブラウザ環境・モバイル環境それぞれにレシート解析補助機能を備え、最終的な支出データをローカルストレージ（SharedPreferences）で永続化・可視化します。

## 概要
本アプリは、「レシート入力の手間を減らす」ことと「正確なデータ管理」を両立する支出管理システムです。
OCRによる自動解析は100%の精度を目指すのではなく、**「AI/アルゴリズムが下書き（Draft）を作り、人間が確認・修正して確定（Confirm）する」**というアプローチ（Human-in-the-Loop）を採用し、ユーザーがストレスなく、かつ正確に家計簿をつけられる環境を提供します。

Flutterのワンコードにより、Webブラウザ環境（Tesseract.js連携）とスマートフォン環境（Google ML Kit連携）のマルチプラットフォームでシームレスに動作します。

---

## 機能要件
1. **支出の手動登録・編集確認機能**
   * 日付、店舗名、品名（複数行対応）、金額、カテゴリを管理・確認して登録できること。日付入力はカレンダーUI（showDatePicker）による選択を強制し、表記のブレを防止。
2. **Web/モバイル両対応のレシート画像解析補助機能（マルチエンジン対応）**
   * **Webブラウザ環境:** 外部JavaScriptライブラリ（Tesseract.js）を非同期インターオペラビリティ（dart:js）を介して呼び出し、日本語OCRを実行。自動ページセグメンテーションモードを最適化し、文字崩れを防止。
   * **モバイル環境:** Google ML Kit（TextRecognizer）を使用してオンデバイスで超高速・高精度な日本語文字認識を実行。
3. **データ検証（バリデーション）**
   * 必須項目の未入力や、金額への不適切な数値（マイナス値や非数値など）に対して、Flutter Formによるバリデーションを行い、適切なエラーメッセージを表示すること。
4. **SharedPreferencesによる高速なローカル永続化＆高度な履歴管理**
   * 確定した支出データをローカルの `SharedPreferences` にJSONエンコードして永続化・追記保存すること。
   * 履歴一覧では、**「新しい順」「金額順」「日付順」の双方向（昇順/降順）ソルト機能**、および**複数項目の一括選択・削除機能（削除モード）**を搭載。
5. **インタラクティブな集計・可視化ダッシュボード機能**
   * 期間フィルター（週単位・月単位・年単位・全期間）のラジオボタンとプルダウンを連動させ、選択期間の「累計総支出額」「選択期間の支出」「1日平均支出」を動的にリアルタイム計算。
   * **前期比（先週比/先月比/前年比）の差額と％（例: -3,500円 (-12.5%)）の算出。**
   * 動的な「支出カレンダー（カレンダーグリッド）」を独自実装。日付ごとのカテゴリ別支出金額をマッピングし、3種類以上のカテゴリ重複時は「内訳表示ダイアログ」をポップアップ。カレンダーのタップで週単位集計へ自動連動。
   * `fl_chart` パッケージを用いた視覚的でリッチな「カテゴリ割合円グラフ（Pizza Chart）」を動的表示。

---

## サブ機能一覧
* **UI（ユーザーインターフェース）部**
  * マテリアルデザイン（Tealカラーベース）を採用したメイン2タブ構成（📥レシート登録・入力 / 📊支出集計ダッシュボード）。
  * 共通画像プレビューコンポーネント（Web/モバイル共通の `Uint8List` メモリレンダリング）。
  * 双方向ソート付き履歴ビュー ＆ チェックボックス式一括削除モード。
  * 4メトリクス・ダッシュボード（累計、期間支出、日平均、前期比）。
  * マトリクス型・多機能支出カレンダー。
* **レシート解析・パース部**
  * テキストの平坦構造化処理（横一行ごとのlines配列抽出）。
  * 店名特定パースアルゴリズム（先頭n行の不要ノイズ・日付・電話番号等のキーワード除外、ブランチ店名結合、「ナインイレブン」ブレ補正等）。
  * 品名・日付・合計金額パースアルゴリズム（「計」「合」に反応する金額抽出、スマホ誤認識「預り/釣/務り」等の除外、数量・単価部分のRegExp前方一致カットトリミング）。
* **データ管理・ストレージ部**
  * `LocalDataManager` クラスによる抽象化。
  * JSONシリアライズ/デシリアライズ（`toMap` / `fromMap`）。
  * インデックス逆順ソートによる安全な複数レコード同時削除アルゴリズム。

---

## 作らないもの（スコープ外）
本プロジェクトの期間内では、以下の機能は実装対象外（スコープ外）とします。
* **ユーザー認証・アカウント管理機能**（ローカル環境での単一ユーザー利用を前提とするため、ログイン画面やマルチユーザー対応は行わない）
* **クラウドデータベース（RDB/NoSQL）の構築**（オフライン・クライアントサイド完結のため、外部サーバー連携は行わない）
* **高精度な汎用レシート解析**（あらゆるレシートへの対応は目指さず、特定のフォーマットや、一定の明瞭さを持つ画像にターゲットを限定する）
* **複数資産（口座・クレジットカード）の連携・管理**（現金や一括の支出管理のみに特化する）

---

## 設計図（Mermaid）

### ユースケース図
```mermaid
graph TD
    %% アクターの定義
    ユーザー([👤 ユーザー])
    
    %% ユースケースの定義
    subgraph 支出管理アプリ
        UC_レシート登録[📥 レシート登録・入力]
        UC_画像アップロード[レシート画像アップロード]
        UC_文字解析[AI文字解析を実行]
        UC_手動修正[手動入力 / 内容確認]
        UC_保存[データを保存する]
        
        UC_履歴閲覧[📋 登録データ履歴の閲覧]
        UC_並び替え[データの並び替え]
        UC_データ削除[データの削除]
        
        UC_ダッシュボード[📊 支出集計ダッシュボード表示]
        UC_期間指定[期間指定 週/月/年/全期間]
        UC_カレンダー閲覧[📅 支出カレンダー閲覧]
        UC_内訳ダイアログ[内訳ダイアログ表示]
        UC_割合グラフ[🍕 カテゴリ割合グラフ表示]
    end

    %% アクターとユースケースの関連
    ユーザー --> UC_レシート登録
    ユーザー --> UC_履歴閲覧
    ユーザー --> UC_ダッシュボード

    %% 包含 (include) / 拡張 (extend) 関係の定義
    UC_レシート登録 -->|包含| UC_保存
    UC_画像アップロード -.->|拡張| UC_レシート登録
    UC_画像アップロード -->|包含| UC_文字解析
    UC_手動修正 -.->|拡張| UC_レシート登録
    
    UC_履歴閲覧 -->|包含| UC_並び替え
    UC_履歴閲覧 -.->|拡張| UC_データ削除
    
    UC_ダッシュボード -->|include| UC_期間指定
    UC_ダッシュボード -->|include| UC_カレンダー閲覧
    UC_ダッシュボード -->|include| UC_割合グラフ
    UC_カレンダー閲覧 -.->|extend| UC_内訳ダイアログ
```

### クラス図

```mermaid
classDiagram
    direction TB

    class アプリ画面 {
        +アプリ起動()
        -レシート入力画面表示()
        -ダッシュボード表示()
    }

    class レシート入力画面 {
        -画像選択状態
        -解析中状態フラグ
        -選択された並び替え基準
        -選択された並び替え順序
        -一括削除モード状態
        -削除選択項目一覧
        +日付選択カレンダー表示()
        +レシート画像解析依頼()
        +履歴並び替え実行()
    }

    class ダッシュボード表示 {
        -選択された集計範囲
        -選択された集計年月
        -グラフ集計範囲
        -集計指標カード表示()
        -支出カレンダーグリッド表示()
    }

    class 支出管理 {
        +支出登録()
        +一括データ削除()
        +全支出履歴の取得()
    }

    class レシート解析 {
        -対応環境判定
        +レシート文字認識解析()
    }

    class ウェブOCR処理 {
        +日本語文字認識()
    }

    class モバイルOCR処理 {
        +日本語文字認識()
    }

    class 支出下書き {
        +日付
        +店舗名
        +品名
        +金額
        +カテゴリ
    }

    class 支出データ {
        +日付
        +店舗名
        +品名
        +金額
        +カテゴリ
    }

    class ローカルストレージ管理 {
        -保存用キー
        +保存データ書込()
        +全データ読込()
        +選択データ一括削除()
    }

    %% 関係性の定義
    アプリ画面 --> レシート入力画面 : 入力・履歴管理タブを生成
    アプリ画面 --> ダッシュボード表示 : 集計ダッシュボードタブを生成

    レシート入力画面 --> 支出管理 : 支出登録・一括削除を依頼
    レシート入力画面 --> レシート解析 : レシートの解析を依頼

    ダッシュボード表示 --> 支出管理 : 集計用の全履歴を要求

    支出管理 --> ローカルストレージ管理 : 保存・読込・一括削除処理
    支出管理 --> 支出データ : 実データを管理・生成

    レシート解析 *-- ウェブOCR処理 : ブラウザ環境で利用
    レシート解析 *-- モバイルOCR処理 : スマホ環境で利用

    レシート解析 ..> 支出下書き : 抽出結果から下書きを生成
    ローカルストレージ管理 ..> 支出データ : 相互変換・永続化保存
```

### シーケンス図

```mermaid
sequenceDiagram
    autonumber
    actor ユーザー as 👤 ユーザー
    box RGB(240, 242, 246) 画面UI層
        participant UI as 入力タブ (画面)
    end
    box RGB(220, 235, 230) コントローラ・処理層
        participant Ctrl as 入力タブ状態 / ウェブOCR処理
    end
    box RGB(210, 220, 240) モデル・ストレージ層
        participant Mgr as ローカルデータマネージャー
        participant DB as 内部ストレージ (SharedPreferences)
    end

    ユーザー ->> UI: 「ギャラリー/カメラから選択」をタップ
    UI ->> Ctrl: 画像取得トリガー
    Ctrl -->> UI: 画像データファイルの返却
    UI ->> Ctrl: レシート画像解析処理の呼び出し
    activate Ctrl
    Ctrl ->> UI: 解析中フラグ = 真 (ロード表示開始)
    
    alt ウェブブラウザ環境の場合
        Ctrl ->> Ctrl: ウェブ側文字認識処理 (Tesseract.js)
    else スマートフォン環境の場合
        Ctrl ->> Ctrl: モバイル側文字認識処理 (ML Kit)
    end
    
    Ctrl ->> Ctrl: テキスト抽出・店名/日付/金額の自動パース
    Ctrl ->> UI: 各入力フォームに解析結果を反映
    Ctrl ->> UI: 解析中フラグ = 偽 (ロード表示終了)
    deactivate Ctrl

    ユーザー ->> UI: 内容を確認して「データを保存する」をタップ
    UI ->> Ctrl: 入力フォームのバリデーション検証
    
    alt バリデーション成功 (正常値)
        Ctrl ->> Mgr: 支出データの保存を依頼
        activate Mgr
        Mgr ->> DB: 全支出データの読み込み
        DB -->> Mgr: 既存のJSON文字列リスト
        Mgr ->> Mgr: 新規支出データを追加 ＆ JSONシリアライズ
        Mgr ->> DB: 更新データの書き込み保存
        DB -->> Mgr: 保存成否の結果 (真/偽)
        Mgr -->> Ctrl: 保存成功通知
        deactivate Mgr
        
        Ctrl ->> UI: 入力フォームのクリア ＆ 成功メッセージ表示
        Ctrl ->> UI: データ再読込通知() (親画面のリスト更新)
    else バリデーション失敗 (エラー時)
        Ctrl ->> ユーザー: 画面にエラーメッセージを表示
    end
```

### 状態遷移図
```mermaid
stateDiagram-v2
    title アプリ内状態およびデータライフサイクル遷移

    [*] --> 初期化完了 : アプリ起動 / タブの初期化
    
    state 入力フォームの状態遷移 {
        初期化完了 --> 空のフォーム : 画面表示 (当日日付のみ自動入力)
        
        空のフォーム --> AI解析中 : レシート画像をアップロード
        AI解析中 --> 解析結果入力済み : OCR文字解析が正常完了
        
        解析結果入力済み --> AI解析中 : 別の新しいレシート画像を再選択
        解析結果入力済み --> 空のフォーム : データを保存 / 入力欄クリア
        解析結果入力済み --> 解析結果入力済み : ユーザーによる手動の修正
        
        空のフォーム --> 解析結果入力済み : ユーザーが最初から手動入力
    }

    state 履歴データ表示の状態遷移 {
        [*] --> 通常一覧表示 : 保存済みデータの表示
        通常一覧表示 --> 削除モード : 「🗑 削除モード」ボタンをタップ
        
        state 削除モード {
            [*] --> 削除待機状態
            削除待機状態 --> 削除項目選択中 : チェックボックスをオン
            削除項目選択中 --> 削除項目選択中 : 他のチェックを追加 / 解除
            削除項目選択中 --> 削除待機状態 : すべてのチェックを外す
            
            削除項目選択中 --> 削除処理実行中 : 「🔴 削除」ボタンをタップ
            削除処理実行中 --> 削除待機状態 : ストレージ内データ削除 ＆ 一覧更新
        }
        
        削除モード --> 通常一覧表示 : 「✕」で閉じる / 削除が完了して通常表示に戻る
    }
    
    解析結果入力済み --> [*] : アプリ終了
    通常一覧表示 --> [*] : アプリ終了
```